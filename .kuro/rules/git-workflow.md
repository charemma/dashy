# Git workflow

- Feature branches, never commit to main
- Conventional commits: `feat(scope): description`, `fix(scope):`, `test(scope):`
- One issue per branch, one PR per issue
- Draft PRs with `Closes #N` in the body
